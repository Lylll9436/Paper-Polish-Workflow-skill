---
phase: 16-body-generation-bilingual-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/body-generation-rules.md
  - .claude/skills/repo-to-paper-skill/SKILL.md
autonomous: true
requirements: [REPO-06, REPO-07, BILN-01]

must_haves:
  truths:
    - "Step 5 body generation workflow exists in repo-to-paper-skill SKILL.md"
    - "Generated body text includes [SOURCE: file:line] annotations on repo-derived claims"
    - "Output uses CEUS journal template formatting (past tense for methods/results, cautious verbs, no promotional language)"
    - "Bilingual output mode produces paragraph-by-paragraph English + Chinese comparison using LaTeX comment format"
    - "Quantitative claims without supporting repo data use explicit placeholders ([RESULTS NEEDED], [EXACT VALUE: metric])"
    - "Citations use \\cite{key} with keys strictly from .paper-refs/ files; unsupported claims use [CITATION NEEDED]"
    - "After all selected sections confirmed, references.bib is auto-generated from .paper-refs/ BibTeX blocks"
  artifacts:
    - path: "references/body-generation-rules.md"
      provides: "Anti-hallucination rules, CEUS style rules, bilingual LaTeX format, references.bib algorithm, placeholder vocabulary"
      min_lines: 80
    - path: ".claude/skills/repo-to-paper-skill/SKILL.md"
      provides: "Step 5 body generation workflow appended after Step 4"
      contains: "### Step 5"
  key_links:
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "references/body-generation-rules.md"
      via: "references.required in frontmatter and Step 5 load instruction"
      pattern: "body-generation-rules\\.md"
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "references/journals/ceus.md"
      via: "references.required in frontmatter (moved from leaf_hints)"
      pattern: "references/journals/ceus\\.md"
    - from: "Step 5 body generation"
      to: ".paper-refs/*.md"
      via: "reads citation keys and BibTeX for \\cite{} embedding and references.bib generation"
      pattern: "\\.paper-refs/"
    - from: "Step 5 body generation"
      to: ".paper-output/*.tex"
      via: "writes per-H1 section .tex files"
      pattern: "\\.paper-output/"
---

<objective>
Add Step 5 (Body Generation) to the repo-to-paper-skill with a supporting reference file containing all generation rules.

Purpose: Enable users to generate complete section body text from confirmed H3 outlines, with evidence annotations, CEUS journal formatting, citation integration from .paper-refs/, and bilingual paragraph-by-paragraph comparison -- completing the repo-to-paper pipeline from scan to publishable draft.

Output: `references/body-generation-rules.md` (generation rules reference file) and updated `.claude/skills/repo-to-paper-skill/SKILL.md` (Step 5 workflow + frontmatter updates)
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-repo-to-paper-core-structure/14-01-SUMMARY.md
@.planning/phases/15-literature-integration/15-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From references/bilingual-output.md (LaTeX comment format):
```latex
% --- Paragraph N ---
% Chinese comment lines here
English paragraph text here.
```
Paragraph markers REQUIRED for paragraph-level bilingual files.
Opt-out keywords: `english only`, `no bilingual`, `only english`, `不要中文`

From references/journals/ceus.md (CEUS writing style):
- Past tense for methods/results
- Cautious verbs: `suggest`, `indicate`, `show`
- Avoid: `novel`, `groundbreaking`, `revolutionary`
- Geography/urban-systems perspective throughout
- 8,000 word limit
- Section Guidance: Abstract, Introduction, Study Area/Data/Methods, Results, Discussion, Conclusion

From .claude/skills/literature-skill/SKILL.md (BibTeX format):
- Citation key: `firstAuthorLastnameLowercaseYYYYfirstKeyword` (e.g., `smith2023urban`)
- Entry types: `@article` (journal), `@inproceedings` (conference), `@misc` (preprint)
- All BibTeX fields MUST come from MCP-returned data; missing fields omitted

From references/repo-patterns.md (category-to-section mapping):
| Category | Primary Section | Secondary Section |
|---|---|---|
| documentation | Introduction | Methods |
| config | Methods | - |
| results | Results and Discussion | - |
| code | Methods | - |
| figures | Results and Discussion | - |
| dependencies | Methods | - |

From .claude/skills/repo-to-paper-skill/SKILL.md (current structure):
- Frontmatter references.required: [references/repo-patterns.md, references/bilingual-output.md]
- Frontmatter references.leaf_hints: [references/journals/ceus.md]
- output_contract: [scan_summary, paper_outline, literature_refs]
- Current line count: 381 lines
- Workflow: Step 1 (Scan) -> Step 2 (H1) -> Step 3 (H2) -> Step 2.5 (Literature) -> Step 4 (H3)
- Step 4 ends at line ~290 (before Output Contract section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/body-generation-rules.md</name>
  <files>references/body-generation-rules.md</files>
  <read_first>
    - references/bilingual-output.md (LaTeX comment format spec to replicate exactly)
    - references/journals/ceus.md (CEUS writing style rules to reference, not duplicate)
    - .claude/skills/literature-skill/SKILL.md (BibTeX citation key format to reuse)
    - .planning/phases/16-body-generation-bilingual-output/16-CONTEXT.md (locked decisions for anti-hallucination rules and citation integration)
    - .planning/phases/16-body-generation-bilingual-output/16-RESEARCH.md (recommended file structure and examples)
    - references/skill-conventions.md (line budget context -- this file is extracted to keep SKILL.md lean)
  </read_first>
  <action>
Create `references/body-generation-rules.md` with the following sections and exact content:

**File header:**
```
# Body Generation Rules

Rules for generating academic paper body text from repo analysis. Used by `repo-to-paper-skill` Step 5.

Scope: Anti-hallucination annotations, claim classification, bilingual LaTeX formatting, and references.bib generation.
```

**Section 1: Claim Classification Table**

Create a 4-row table defining claim types, annotation requirements, and examples:

| Claim Type | Annotation Required | Example |
|---|---|---|
| Repo-derived (specific numbers, model names, dataset sizes, configurations) | `[SOURCE: file:line]` after the claim | `The model uses 500 estimators [SOURCE: config.yaml:12]` |
| Quantitative result (metric value unknown) | `[RESULTS NEEDED]` placeholder | `The model achieved an R-squared of [RESULTS NEEDED]` |
| Specific metric (metric name known, value unknown) | `[EXACT VALUE: metric]` placeholder | `The RMSE was [EXACT VALUE: RMSE]` |
| Background/context prose (general domain knowledge, problem framing) | None -- write as normal academic text | `Urban heat islands result from impervious surface replacement` |

Add rule text:
- "Every repo-derived specific claim MUST have a `[SOURCE: file:line]` annotation. The annotation references the actual repo file and line number where the data was found during Step 1 scan or Step 5 content reading."
- "Never state a specific number, model name, dataset size, or configuration value without a `[SOURCE:]` annotation."
- "Background/context prose does not require annotation -- general domain knowledge used for framing and transitions is acceptable."

**Section 2: Citation Rules**

- Heading: `## Citation Integration`
- All citations use `\cite{citationkey}` inline format
- Citation keys MUST only reference papers present in `{repo_path}/.paper-refs/*.md` files
- If a claim needs a citation but no `.paper-refs/` entry supports it: use `[CITATION NEEDED]` placeholder
- When `.paper-refs/` directory is missing entirely (Step 2.5 was skipped): ALL citation positions use `[CITATION NEEDED]`
- Never cite from Claude's background knowledge -- this is a strict source boundary

**Section 3: Bilingual LaTeX Format**

- Heading: `## Bilingual LaTeX Format`
- State: "Per `references/bilingual-output.md`, body text uses LaTeX comment format with paragraph markers."
- Include the exact template:
```latex
% --- Paragraph 1 ---
% Chinese academic translation of the paragraph below.
% Technical terms preserved with English in parentheses: 城市热岛效应（urban heat island effect）.
English paragraph text with \cite{key} citations and [SOURCE: file:line] annotations.

% --- Paragraph 2 ---
% Next Chinese paragraph.
Next English paragraph.
```
- Rules:
  - Paragraph markers (`% --- Paragraph N ---`) are REQUIRED (per bilingual-output.md)
  - Chinese uses academic written register (学术书面语): "本研究提出了..." not "我们做了..."
  - Technical terms: preserve English with Chinese explanation in parentheses
  - Chinese translation includes the same `\cite{key}` references as the English text
  - When bilingual mode is OFF (opt-out keywords detected): omit all `%` Chinese comment lines and paragraph markers

**Section 4: CEUS Writing Style**

- Heading: `## CEUS Writing Style`
- State: "Full CEUS contract at `references/journals/ceus.md`. Key rules for body generation:"
- Tense: past tense for methods and results sections; present tense acceptable in discussion for broader implications
- Verbs: prefer `suggest`, `indicate`, `show` unless stronger causal evidence is available
- Avoid: `novel`, `groundbreaking`, `revolutionary`, or similar self-promotional adjectives
- Avoid: empty smart-city framing not used in the analysis
- Avoid: over-generalized policy claims unsupported by the empirical setting
- Maintain geography/urban-systems perspective throughout

**Section 5: references.bib Generation**

- Heading: `## references.bib Generation`
- Algorithm (numbered steps):
  1. Read all files matching `{repo_path}/.paper-refs/*.md`
  2. For each file, extract all content between ` ```bibtex ` and ` ``` ` markers
  3. Parse each BibTeX entry to extract the citation key (the identifier after `@article{`, `@inproceedings{`, etc.)
  4. Deduplicate: if the same citation key appears in multiple files, keep the first occurrence only
  5. Write all unique entries to `{repo_path}/.paper-output/references.bib`
  6. Display count: "Generated references.bib with N unique entries from M ref files."
- Timing rule: "Generate references.bib ONCE after ALL selected sections are confirmed. Do NOT generate per-section."

**Section 6: Output File Structure**

- Heading: `## Output File Structure`
- Directory: `{repo_path}/.paper-output/`
- One `.tex` file per H1 section, named by section topic in lowercase (e.g., `introduction.tex`, `methods.tex`, `results.tex`, `discussion.tex`, `conclusion.tex`)
- Each `.tex` file contains `\section{}`, `\subsection{}`, `\subsubsection{}` LaTeX heading commands followed by body paragraphs
- Include a complete example `.tex` file showing the full format with bilingual comments, `\cite{}`, `[SOURCE:]`, and LaTeX headings

**Footer:**
```
---

*Reference: references/body-generation-rules.md*
*Conventions: references/skill-conventions.md*
```

Target: 100-130 lines. This file contains all detail so SKILL.md Step 5 stays lean.
  </action>
  <verify>
    <automated>grep -c "Claim Type" references/body-generation-rules.md && grep -c "\\[SOURCE:" references/body-generation-rules.md && grep -c "CITATION NEEDED" references/body-generation-rules.md && grep -c "references.bib" references/body-generation-rules.md && grep -c "Paragraph" references/body-generation-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - references/body-generation-rules.md exists
    - File contains `# Body Generation Rules` as first heading
    - File contains `## Claim Classification` or `## Claim Classification Table` section
    - File contains a table row with `[SOURCE: file:line]`
    - File contains a table row with `[RESULTS NEEDED]`
    - File contains a table row with `[EXACT VALUE: metric]`
    - File contains `## Citation Integration` section with `\cite{citationkey}` reference
    - File contains `[CITATION NEEDED]` placeholder documentation
    - File contains `## Bilingual LaTeX Format` section with `% --- Paragraph 1 ---` example
    - File contains `## CEUS Writing Style` section referencing `references/journals/ceus.md`
    - File contains `## references.bib Generation` section with 6-step algorithm
    - File contains "Generate references.bib ONCE after ALL selected sections are confirmed"
    - File contains `## Output File Structure` section with `.paper-output/` directory reference
    - File contains `\section{}` or `\section{` in the output example
    - File is between 80 and 150 lines
  </acceptance_criteria>
  <done>Reference file exists with all 6 sections: claim classification, citation rules, bilingual format, CEUS style, references.bib algorithm, and output file structure. All placeholder vocabulary documented. Complete LaTeX output example included.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 5 to repo-to-paper-skill SKILL.md</name>
  <files>.claude/skills/repo-to-paper-skill/SKILL.md</files>
  <read_first>
    - .claude/skills/repo-to-paper-skill/SKILL.md (current 381-line file -- read ENTIRE file to understand structure)
    - references/body-generation-rules.md (just created in Task 1 -- verify it exists and understand what it covers)
    - .planning/phases/16-body-generation-bilingual-output/16-CONTEXT.md (locked decisions for Step 5 interaction flow)
    - references/skill-conventions.md (line budget, AskUserQuestion enforcement rules)
    - references/bilingual-output.md (opt-out keyword list for bilingual mode reference)
  </read_first>
  <action>
Modify `.claude/skills/repo-to-paper-skill/SKILL.md` with these specific changes:

**1. Frontmatter updates (lines 1-35):**

Update `description` to add body generation mention:
```yaml
description: >-
  Scan an experiment repo and generate a complete paper outline (H1/H2/H3)
  with user approval checkpoints at each level, then generate body text
  with evidence annotations, citations, and bilingual output. Python ML repos.
  扫描实验仓库，逐级生成论文大纲（H1/H2/H3），每级用户确认后推进，
  然后生成带证据标注、引用和双语输出的正文文本。
```

Update `references.required` to add two entries:
```yaml
references:
  required:
    - references/repo-patterns.md
    - references/bilingual-output.md
    - references/body-generation-rules.md
    - references/journals/ceus.md
  leaf_hints: []
```
Note: `journals/ceus.md` moves from `leaf_hints` to `required` because Step 5 always needs CEUS formatting. `leaf_hints` becomes empty list `[]`.

Update `output_contract` to add body_text:
```yaml
output_contract:
  - scan_summary
  - paper_outline
  - literature_refs
  - body_text
```

**2. Add Step 5 after Step 4 (insert before `## Output Contract` section):**

Insert `### Step 5: Body Generation` with this exact content (~45-50 lines):

```markdown
---

### Step 5: Body Generation

Step 5 auto-continues in the same session after H3 confirmation. It uses the H3 outline already in memory -- does not re-read `paper_outline.md`.

**Prepare:**
- Load `references/body-generation-rules.md` and `references/journals/ceus.md`
- Check if `{repo_path}/.paper-refs/` exists. If missing: note that all citation positions will use `[CITATION NEEDED]`
- Create `{repo_path}/.paper-output/` directory if it does not exist

**Section selection:**
- Display all H1 sections via AskUserQuestion (multiSelect):

  ```
  AskUserQuestion({
    question: "Which sections should I generate body text for?",
    options: [
      { label: "1. [H1 title]", description: "[H2 subsection summary]" },
      ...one option per H1 section from confirmed outline
    ]
  })
  ```

- User selects which sections to generate in this session

**Generation loop (for each selected H1 section, sequentially):**

1. Read relevant repo files using the Category to Paper Section Mapping from `references/repo-patterns.md`
2. Read `{repo_path}/.paper-refs/{section}.md` if it exists -- extract `\cite{key}` citation keys
3. Generate full section `.tex` content following ALL rules in `references/body-generation-rules.md`:
   - `\section{}`, `\subsection{}`, `\subsubsection{}` LaTeX heading commands from confirmed H2/H3 structure
   - Academic prose following CEUS writing style from `references/journals/ceus.md`
   - `\cite{key}` inline citations (keys from `.paper-refs/` only; `[CITATION NEEDED]` for unsupported claims)
   - `[SOURCE: file:line]` annotations on all repo-derived claims (specific numbers, configs, model names)
   - `[RESULTS NEEDED]` / `[EXACT VALUE: metric]` for unknown quantitative data
   - Bilingual: `% --- Paragraph N ---` markers + `%` Chinese comment lines before each English paragraph (skip if opt-out detected)
4. Write to `{repo_path}/.paper-output/{section}.tex`
5. Display the generated section content to the user
6. AskUserQuestion for confirmation:

   ```
   AskUserQuestion({
     question: "Section [N]: [Title] generated. Please review above.",
     options: [
       { label: "Confirm", description: "Accept this section and proceed to next" },
       { label: "Modify", description: "Describe changes needed (will regenerate entire section)" },
       { label: "Skip", description: "Skip this section, move to next" }
     ]
   })
   ```

   - **Confirm:** proceed to next selected section
   - **Modify:** user describes changes -> regenerate entire section -> re-display -> loop until confirmed
   - **Skip:** move to next section without writing file

**After all selected sections confirmed:**
- Generate `references.bib` following the algorithm in `references/body-generation-rules.md`
- Display completion summary: number of sections generated, files written, references.bib entry count
```

**3. Update Output Contract table (add body_text row):**

Add this row to the existing Output Contract table:
```
| `body_text` | Per-H1 `.tex` files in `{repo_path}/.paper-output/` + `references.bib` | After Step 5 sections confirmed |
```

**4. Update Edge Cases table (add 3 rows):**

Add these rows:
```
| .paper-refs/ directory missing | Continue generating; all citation positions use `[CITATION NEEDED]` placeholders; do not block |
| User selects zero sections in Step 5 | Skip body generation entirely; display "No sections selected. Body generation skipped." |
| Modification loop exceeds 3 iterations | Proceed with current version; warn "Multiple revision cycles detected. Consider manual editing for fine adjustments." |
```

**5. Update Fallbacks table (add 1 row):**

Add this row:
```
| `references/body-generation-rules.md` missing | Refuse: "Required reference file references/body-generation-rules.md not found. Cannot generate body text." |
```

**6. Update Modes table:**

Change `guided` mode description to reflect the full workflow:
```
| `guided` | Yes | Full 5-step workflow with confirmation at each heading level and section-by-section body generation |
```

**Line budget target:** The SKILL.md should end up at approximately 420-440 lines (381 current + ~40-50 for Step 5 + ~10 for edge cases/fallbacks/output contract updates). This exceeds the 300-line convention but is justified: the Skill is the most complex in the project (5 steps + 1 decimal step) and generation rules are already extracted to a reference file.

Do NOT modify the existing Steps 1-4 or Step 2.5 content. Do NOT modify the Examples section.
  </action>
  <verify>
    <automated>grep -n "### Step 5" .claude/skills/repo-to-paper-skill/SKILL.md && grep -c "body_text" .claude/skills/repo-to-paper-skill/SKILL.md && grep "body-generation-rules.md" .claude/skills/repo-to-paper-skill/SKILL.md && grep "leaf_hints" .claude/skills/repo-to-paper-skill/SKILL.md && wc -l .claude/skills/repo-to-paper-skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `### Step 5: Body Generation` heading
    - SKILL.md frontmatter `output_contract` contains `body_text`
    - SKILL.md frontmatter `references.required` contains `references/body-generation-rules.md`
    - SKILL.md frontmatter `references.required` contains `references/journals/ceus.md`
    - SKILL.md frontmatter `leaf_hints` is empty list `[]`
    - SKILL.md contains `AskUserQuestion` in Step 5 (at least 2 occurrences: section selection + confirmation)
    - SKILL.md Step 5 contains `multiSelect` for section selection
    - SKILL.md Step 5 contains `Confirm` and `Modify` and `Skip` options
    - SKILL.md Step 5 references `.paper-output/` directory
    - SKILL.md Step 5 references `.paper-refs/` for citation keys
    - SKILL.md Step 5 contains `references.bib` generation instruction
    - SKILL.md Output Contract table contains `body_text` row
    - SKILL.md Edge Cases contains `.paper-refs/ directory missing` row
    - SKILL.md Fallbacks contains `body-generation-rules.md missing` row
    - SKILL.md description mentions body text generation (English and Chinese)
    - SKILL.md total line count is between 410 and 450
    - Existing Steps 1-4 and Step 2.5 are UNCHANGED (verify by checking Step 1 heading still at same approximate line)
  </acceptance_criteria>
  <done>SKILL.md has Step 5 body generation workflow with AskUserQuestion section selection (multiSelect) and per-section confirmation (Confirm/Modify/Skip). Frontmatter updated: body_text in output_contract, body-generation-rules.md and journals/ceus.md in required references, leaf_hints empty. Edge cases and fallbacks updated. Total file ~420-440 lines.</done>
</task>

</tasks>

<verification>
1. `references/body-generation-rules.md` exists with all 6 required sections (claim classification, citation rules, bilingual format, CEUS style, references.bib algorithm, output file structure)
2. `.claude/skills/repo-to-paper-skill/SKILL.md` contains Step 5 after Step 4 and before Output Contract
3. SKILL.md frontmatter has `body-generation-rules.md` in `references.required` and `body_text` in `output_contract`
4. `journals/ceus.md` moved from `leaf_hints` to `required` in SKILL.md frontmatter
5. Step 5 uses AskUserQuestion for both section selection (multiSelect) and per-section confirmation (Confirm/Modify/Skip)
6. Step 5 references body-generation-rules.md for all detailed rules (not inlined)
7. All three requirement IDs covered: REPO-06 (SOURCE annotations), REPO-07 (CEUS formatting), BILN-01 (bilingual paragraph comparison)
</verification>

<success_criteria>
- Complete repo-to-paper-skill workflow: Steps 1 -> 2 -> 3 -> 2.5 -> 4 -> 5
- Body generation rules extracted to reference file, SKILL.md stays under 450 lines
- All locked decisions from 16-CONTEXT.md implemented (section selection via multiSelect, per-section Confirm/Modify/Skip, auto-continue after H3, .paper-output/ directory, LaTeX comment bilingual format, references.bib at end, strict .paper-refs/ citation boundary, anti-hallucination placeholders)
- No deferred items implemented (none were deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/16-body-generation-bilingual-output/16-01-SUMMARY.md`
</output>
