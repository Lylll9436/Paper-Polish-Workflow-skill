---
phase: 14-repo-to-paper-core-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/repo-patterns.md
  - .claude/skills/repo-to-paper-skill/SKILL.md
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-05
  - REPO-08

must_haves:
  truths:
    - "references/repo-patterns.md contains file identification patterns table mapping glob patterns to categories with priority ordering"
    - "references/repo-patterns.md contains category-to-paper-section mapping table linking file categories to IMRaD sections"
    - "SKILL.md has a 4-step workflow with user confirmation checkpoint between each heading level"
    - "SKILL.md workflow Step 1 scans repo top-2-levels and presents categorized summary table"
    - "SKILL.md workflow Step 2 generates H1 from journal template or IMRaD default with user confirmation loop"
    - "SKILL.md workflow Step 3 generates H2 by reading actual file contents with source annotations and user confirmation loop"
    - "SKILL.md workflow Step 4 generates H3 by reading actual file contents with source annotations and user confirmation loop"
    - "SKILL.md stays within ~320 line budget"
  artifacts:
    - path: "references/repo-patterns.md"
      provides: "Scan heuristics and section mapping rules for Python ML repos"
      contains: "File Identification Patterns"
    - path: ".claude/skills/repo-to-paper-skill/SKILL.md"
      provides: "Multi-checkpoint repo-to-paper outline Skill"
      exports: ["repo-to-paper-skill"]
      min_lines: 250
  key_links:
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "references/repo-patterns.md"
      via: "references.required in frontmatter + Read tool in workflow Step 1"
      pattern: "references/repo-patterns\\.md"
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "references/bilingual-output.md"
      via: "references.required in frontmatter"
      pattern: "references/bilingual-output\\.md"
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "references/journals/ceus.md"
      via: "references.leaf_hints in frontmatter + conditional load in workflow Step 2"
      pattern: "references/journals/"
---

<objective>
Create the repo-to-paper Skill and its companion reference file. The Skill enables users to point at an experiment repository and get a complete H1/H2/H3 paper outline with approval checkpoints at each heading level. The reference file extracts scan heuristics for maintainability.

Purpose: This is the core new Skill for v2.0 — it bridges the gap between experiment code and academic paper structure. Separating scan heuristics into a reference file keeps the Skill lean and the patterns maintainable.

Output: Two new files — `references/repo-patterns.md` (~100 lines) and `.claude/skills/repo-to-paper-skill/SKILL.md` (~300-320 lines)
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@references/skill-conventions.md
@references/skill-skeleton.md
@references/bilingual-output.md
@references/journals/ceus.md
@.claude/skills/experiment-skill/SKILL.md

@.planning/phases/14-repo-to-paper-core-structure/14-CONTEXT.md
@.planning/phases/14-repo-to-paper-core-structure/14-RESEARCH.md

<interfaces>
<!-- Key conventions the executor must follow. Extracted from project files. -->

From references/skill-conventions.md:
- YAML frontmatter: name, description, triggers, tools, references, input_modes, output_contract
- Body sections (all required): Purpose, Trigger, Modes, References, Ask Strategy, Workflow, Output Contract, Edge Cases, Fallbacks
- Line budget: ~300 lines target, ~320 acceptable with justification
- Tools listed by capability: Read, Write, Structured Interaction
- Bilingual-eligible Skills: must reference bilingual-output.md, support opt-out keywords

From references/skill-skeleton.md:
- Copyable skeleton template defines section order and formatting conventions
- Modes table format: Mode | Default | Behavior
- References split into Required (always loaded) and Leaf Hints (loaded when needed)
- Edge Cases and Fallbacks use Situation | Handling tables

From .claude/skills/experiment-skill/SKILL.md:
- Two-phase checkpoint pattern: Phase 1 analyze -> present -> user confirms -> Phase 2 generate
- Confirmation line format: "Identified N findings. Please confirm, correct, or add before I write discussion."
- Phase separation with --- horizontal rule
- "Step N — Action:" formatting within phases
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/repo-patterns.md with scan heuristics and section mapping</name>
  <files>references/repo-patterns.md</files>
  <read_first>
    - references/bilingual-output.md (structural model — similar ~100-line flat reference file)
    - references/journals/ceus.md (section guidance — the Section Guidance headings define what categories map to)
    - .planning/phases/14-repo-to-paper-core-structure/14-CONTEXT.md (locked decisions on content and format)
    - .planning/phases/14-repo-to-paper-core-structure/14-RESEARCH.md (example structure in "repo-patterns.md Structure" section)
  </read_first>
  <action>
Create `references/repo-patterns.md` (~100 lines) following the `bilingual-output.md` flat-file pattern. The file has two main parts:

**File structure:**
```
# Repo Scan Patterns
[1-2 sentence purpose statement: scan heuristics for Python ML experiment repositories]
[1 line: scope limitation — Python ML projects only, see ADVR-01 for expansion]

## Quick Reference
[Summary table: Category | Primary Section | Key File Types — one row per category, 6 categories total]

## File Identification Patterns
[Table with columns: Glob | Category | Priority | Notes]
Include these patterns (Priority 1 = highest):
- Priority 1 (documentation): README.md, README.rst, README.txt, docs/**/*.md
- Priority 2 (config): *.yaml, *.yml, *.json, *.toml, *.ini, *.cfg, .env.example, Makefile
- Priority 3 (results): results/**/*.csv, results/**/*.json, results/**/*.xlsx, results/**/*.tsv, logs/**/*.log, *.csv (root only), metrics.json, scores.json
- Priority 4 (code): *.py, src/**/*.py, scripts/**/*.py, *.ipynb (see Ambiguity Rules for notebooks)
- Priority 5 (figures): figures/**/*.png, figures/**/*.jpg, figures/**/*.svg, figures/**/*.pdf, plots/**/*.*, *.png, *.jpg, *.svg (root only)
- Priority 6 (dependencies): requirements.txt, setup.py, setup.cfg, pyproject.toml, Pipfile, environment.yml, conda.yml

## Category to Paper Section Mapping
[Table with columns: Category | Primary Section | Secondary Section | Notes]
Rows:
- documentation | Introduction | Methods | README often describes both motivation and approach
- config | Methods | - | Hyperparameters, model architecture choices, experiment settings
- results | Results and Discussion | - | Tables, metrics, evaluation outputs
- code | Methods | - | Algorithm implementation details, model architecture
- figures | Results and Discussion | - | Visualizations of findings, plots
- dependencies | Methods | - | Technology stack, reproducibility information

## Ambiguity Rules
[Bullet list of disambiguation rules:]
- Jupyter notebooks (*.ipynb): categorize as "code" by default. If located in results/, figures/, or output/ directory, categorize by directory instead
- Files matching multiple categories: use the LOWEST priority number (most specific wins)
- Hidden files and directories (.*): skip entirely
- Test files (test_*, *_test.py, tests/): skip — not relevant to paper content
- Data files (*.h5, *.pkl, *.npy, *.pt, *.pth): skip — binary data, not directly readable for outline generation
- Scan depth: top 2 levels only (root + first-level subdirectories). User can manually specify deeper paths

## Scan Depth
[Brief note: scan root directory and one level of subdirectories. List directory-level patterns that indicate relevant content: src/, scripts/, results/, figures/, logs/, data/, models/, notebooks/, experiments/]

Footer: single line with file path and reference to skill-conventions.md
```

Target ~100 lines. Use markdown tables for both main sections (per locked decision). Include Notes columns for rationale.
  </action>
  <verify>
    <automated>test -f references/repo-patterns.md && wc -l < references/repo-patterns.md && grep -c "Category\|Priority\|Glob\|Section" references/repo-patterns.md</automated>
  </verify>
  <acceptance_criteria>
    - references/repo-patterns.md exists
    - File contains "## File Identification Patterns" heading
    - File contains "## Category to Paper Section Mapping" heading
    - File contains "## Ambiguity Rules" heading
    - File contains table with columns including "Glob" and "Category" and "Priority"
    - File contains table with columns including "Category" and "Primary Section"
    - File contains 6 categories: documentation, config, results, code, figures, dependencies
    - File contains "Python ML" scope limitation text
    - Line count is between 80 and 120 lines
    - File ends with footer line referencing skill-conventions.md
  </acceptance_criteria>
  <done>references/repo-patterns.md exists with file identification patterns table (6 categories, priority-ordered), category-to-section mapping table, ambiguity rules, and is ~100 lines</done>
</task>

<task type="auto">
  <name>Task 2: Create repo-to-paper-skill SKILL.md with 4-step checkpoint workflow</name>
  <files>.claude/skills/repo-to-paper-skill/SKILL.md</files>
  <read_first>
    - references/repo-patterns.md (just created in Task 1 — verify exact content to reference accurately)
    - references/skill-conventions.md (authoritative Skill structure rules, line budget, bilingual eligibility)
    - references/skill-skeleton.md (copyable skeleton — start from this template)
    - .claude/skills/experiment-skill/SKILL.md (pattern reference for multi-phase checkpoint workflow)
    - references/bilingual-output.md (bilingual format spec — Skill is bilingual-eligible)
    - references/journals/ceus.md (journal template — Skill loads this conditionally for H1 structure)
    - .planning/phases/14-repo-to-paper-core-structure/14-CONTEXT.md (all locked decisions)
    - .planning/phases/14-repo-to-paper-core-structure/14-RESEARCH.md (architecture patterns, pitfalls, code examples)
  </read_first>
  <action>
Create `.claude/skills/repo-to-paper-skill/SKILL.md` (~300-320 lines) starting from the skill-skeleton.md template and following the experiment-skill's multi-phase checkpoint pattern.

**YAML Frontmatter:**
```yaml
---
name: repo-to-paper-skill
description: >-
  Scan an experiment repo and generate a complete paper outline (H1/H2/H3)
  with user approval checkpoints at each level. Python ML repos.
  扫描实验仓库，逐级生成论文大纲（H1/H2/H3），每级用户确认后推进。
triggers:
  primary_intent: generate paper outline from experiment repo
  examples:
    - "Generate a paper outline from my repo"
    - "帮我从实验仓库生成论文大纲"
    - "Scan my project and create paper structure"
    - "把我的代码仓库转成论文框架"
    - "Create paper sections from experiment code"
    - "从代码生成论文结构"
tools:
  - Read
  - Write
  - Structured Interaction
references:
  required:
    - references/repo-patterns.md
    - references/bilingual-output.md
  leaf_hints:
    - references/journals/ceus.md
input_modes:
  - repo_path
output_contract:
  - scan_summary
  - paper_outline
---
```

**Body Sections (follow skeleton order, all required sections):**

**## Purpose** (~4 lines)
This Skill scans a Python ML experiment repository and generates a hierarchical paper outline (H1/H2/H3) with user approval checkpoints at each heading level. It serves researchers who have completed experiments and need to structure their findings into an academic paper. The scan categorizes repo files using patterns from `references/repo-patterns.md`, then progressively generates headings from coarse (sections) to fine (sub-subsections), with the user confirming or modifying each level before proceeding to the next.

**## Trigger** (~8 lines)
Activates when the user asks to generate a paper structure/outline from a repository or codebase. Include trigger examples matching the frontmatter examples list. Include both English and Chinese activations.

**## Modes** (~8 lines)
| Mode | Default | Behavior |
- `guided` (default): Full 4-step workflow with confirmation at each heading level
- `direct`: Not supported — outline generation inherently requires user validation at each level
- `batch`: Not supported — each repo requires unique analysis

**## References** (~12 lines)
Required (always loaded):
| File | Purpose |
| references/repo-patterns.md | File categorization patterns and section mapping rules |
| references/bilingual-output.md | Bilingual output format (Skill is bilingual-eligible) |

Leaf Hints (loaded when needed):
| File | When to Load |
| references/journals/ceus.md | When user specifies CEUS as target journal |

Loading rules: Load required references at Step 1. Load journal template at Step 2 if journal specified. If journal template file is missing, refuse: "Journal template for [X] not found. Available: CEUS."

**## Ask Strategy** (~10 lines)
Before starting, ask about:
1. Repo path (required — "Which repository should I scan?") — use AskUserQuestion if available
2. Target journal (optional — "Which journal is this paper targeting?") — use AskUserQuestion with options: CEUS, Other, None/General. If CEUS selected, load template at Step 2. If Other, ask for journal name. If None, use IMRaD default
3. Bilingual output (inferred — check trigger text for opt-out keywords per bilingual-output.md)

Rules: Never ask more than 2 explicit questions (repo path + journal). Bilingual is detected from trigger, not asked. In direct mode, skip — but direct mode is not supported for this Skill.

**## Workflow** (~120 lines, the main body — keep each step to ~25-30 lines)

### Step 1: Scan Repository

**Prepare:**
- Load `references/repo-patterns.md` for file categorization patterns
- Read the repository directory structure (top 2 levels only: root + first-level subdirectories)
- Categorize each file using the File Identification Patterns table from repo-patterns.md
- Apply ambiguity rules for files matching multiple categories (lowest priority number wins)
- Skip hidden files, test files, and binary data files per repo-patterns.md rules

**Present:**
- Display a categorized summary table with columns: Category | Files Found | Key Items
- Each row lists the category, count of matching files, and 2-3 most important file names
- Mark missing categories with a warning: "⚠ No [category] files found"
- Summary line: "Scanned N files in M categories. Proceeding to H1 outline generation..."
- If user wants to review or correct, they can interrupt; otherwise auto-proceed to Step 2

---

### Step 2: Generate H1 Outline

**Prepare:**
- If journal specified (e.g., CEUS), load `references/journals/[journal].md` and use its Section Guidance headings as H1 structure
- CEUS H1 sections from template: Abstract, Introduction, Study Area / Data / Methods, Results, Discussion, Conclusion
- If no journal specified, use IMRaD default: Introduction, Methods, Results and Discussion, Conclusion
- Adjust base sections based on scan summary (e.g., add "Study Area" if spatial data detected in results, split "Results and Discussion" if large results set)

**Present:**
- Display H1 headings with one-sentence descriptions (title + description format):
  ```
  # 1. Introduction
      Establish research context, literature gap, and contribution statement.
  # 2. Methods
      Describe the analytical approach, data sources, and model architecture.
  ```
- Bilingual: if bilingual mode is ON, add Chinese translation of each one-sentence description using Markdown blockquote format per bilingual-output.md
- Summary line: "Generated N H1 sections. Please confirm, modify, or add before proceeding to H2."
- Wait for user confirmation ("ok") or modification instructions

**On modification:** Revise H1 headings per user feedback and re-display. Loop until confirmed.

---

### Step 3: Generate H2 Outline

**Prepare:**
- For each confirmed H1 section, read relevant repo files to generate H2 sub-headings
- Use the Category to Paper Section Mapping from repo-patterns.md to identify which files inform each H1 section
- Read actual file contents (README for Introduction, config files for Methods, result files for Results, etc.) — H2 generation requires content reading, not just directory structure
- Generate 2-4 H2 sub-headings per H1 section, each with a one-sentence description

**Present:**
- Display full H1 + H2 hierarchy with source annotations on each H2:
  ```
  # 1. Introduction
  ## 1.1 Research Background and Motivation  <- from: README.md
      Urban heat island measurement requires fine-grained spatial analysis approaches.
  ## 1.2 Literature Gap and Contribution  <- from: README.md
      Existing methods lack integration of street-level semantics with thermal data.
  ```
- Source annotations show the 1-3 most directly relevant files (highest-priority files per repo-patterns.md ordering)
- Bilingual: if ON, add Chinese one-sentence descriptions in Markdown blockquote format
- Summary line: "Generated N H2 subsections across M H1 sections. Please confirm, modify, or add before proceeding to H3."
- Wait for user confirmation or modification

**On modification:** Revise and re-display. Loop until confirmed.

---

### Step 4: Generate H3 Outline

**Prepare:**
- For each confirmed H2 subsection, read deeper file contents to generate H3 sub-sub-headings
- H3 captures specific technical details: individual model components, specific metrics, particular datasets, analysis steps
- Not every H2 needs H3 — generate H3 only where repo content supports further breakdown (typically Methods and Results)
- Generate 0-3 H3 entries per H2 subsection

**Present:**
- Display full H1 + H2 + H3 hierarchy with source annotations on H3 entries:
  ```
  ## 1.1 Research Background and Motivation
  ### 1.1.1 Urban Heat Island Measurement Challenges  <- from: README.md:L15-30
      Current approaches to UHI measurement rely on satellite imagery with limited spatial resolution.
  ```
- Bilingual: if ON, add Chinese descriptions in Markdown blockquote format
- Summary line: "Generated N H3 sub-subsections. Please confirm, modify, or add. This completes the outline structure."
- Wait for user confirmation or modification

**On modification:** Revise and re-display. Loop until confirmed.

**## Output Contract** (~10 lines)
| Output | Format | Condition |
| scan_summary | Categorized summary table | Always — Step 1 |
| paper_outline | Hierarchical H1/H2/H3 with descriptions and source annotations | After all steps confirmed |

After final H3 confirmation, offer to save the complete outline to a file using Write tool:
- Default filename: `paper_outline.md` in the repo root
- Include all heading levels with descriptions and source annotations
- Bilingual descriptions included if bilingual mode is ON

**## Edge Cases** (~12 lines)
| Situation | Handling |
| Empty repository (no recognized files) | Refuse: "No scannable files found in [path]. Please verify the repository path." |
| No README.md found | Warn "⚠ No README.md found" in scan summary; proceed with available files; Introduction H2 entries will have limited source annotations |
| No result files found | Warn in scan summary; Results section H2/H3 will use placeholder descriptions: "[RESULTS NEEDED: add result files to populate this section]" |
| Journal specified but template missing | Refuse: "Journal template for [X] not found. Available: CEUS." |
| User provides non-Python repo | Warn: "Scan patterns are calibrated for Python ML projects. Non-Python files may be miscategorized." Proceed with best-effort categorization |
| Very large repo (>500 files in top 2 levels) | List only top 10 files per category in scan summary; note total count |
| All files in one category | Proceed — outline may be unbalanced; user corrects at confirmation step |

**## Fallbacks** (~8 lines)
| Scenario | Fallback |
| Structured Interaction unavailable | Ask repo path and journal as plain-text questions |
| Reference file repo-patterns.md missing | Refuse: "Required reference file references/repo-patterns.md not found. Cannot categorize repo files." |
| bilingual-output.md missing | Proceed with English-only output; warn user |
| Journal template missing | Refuse with locked message (see Edge Cases) |
| Write tool unavailable | Present final outline in conversation; user saves manually |

Footer: single line with Skill name and reference to conventions file:
```
*Skill: repo-to-paper-skill*
*Conventions: references/skill-conventions.md*
```

**Line budget management:**
- Frontmatter: ~35 lines
- Purpose + Trigger: ~12 lines
- Modes: ~8 lines
- References: ~12 lines
- Ask Strategy: ~10 lines
- Workflow (Steps 1-4): ~120 lines (target ~25-30 lines per step)
- Output Contract: ~10 lines
- Edge Cases: ~12 lines
- Fallbacks: ~8 lines
- Footer + blanks: ~10 lines
- **Total estimate: ~237 core + ~60 formatting/blanks = ~300 lines**

Keep workflow steps concise. Do NOT duplicate repo-patterns.md content inline — reference it. For H3 and H2 steps, note "same confirmation loop as Step 2" rather than repeating the full modification loop text. Use the experiment-skill's confirmation line pattern as the model.
  </action>
  <verify>
    <automated>test -f .claude/skills/repo-to-paper-skill/SKILL.md && wc -l < .claude/skills/repo-to-paper-skill/SKILL.md && grep -c "Step 1\|Step 2\|Step 3\|Step 4" .claude/skills/repo-to-paper-skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/repo-to-paper-skill/SKILL.md exists
    - SKILL.md has valid YAML frontmatter with fields: name, description, triggers, tools, references, input_modes, output_contract
    - frontmatter name field equals "repo-to-paper-skill"
    - frontmatter references.required contains "references/repo-patterns.md"
    - frontmatter references.required contains "references/bilingual-output.md"
    - frontmatter references.leaf_hints contains "references/journals/ceus.md"
    - SKILL.md body contains all required sections: "## Purpose", "## Trigger", "## Modes", "## References", "## Ask Strategy", "## Workflow", "## Output Contract", "## Edge Cases", "## Fallbacks"
    - Workflow contains "### Step 1" through "### Step 4" (4 distinct steps)
    - Step 1 contains text about scanning and categorized summary
    - Step 2 contains "H1" and "IMRaD" and confirmation loop text
    - Step 3 contains "H2" and source annotation pattern "<- from:"
    - Step 4 contains "H3" and confirmation text
    - Edge Cases table contains "Journal template" and "No README" entries
    - Fallbacks table contains "repo-patterns.md missing" entry
    - Line count is between 250 and 330 (within budget with flexibility)
    - File ends with footer referencing skill-conventions.md
    - File contains bilingual opt-out reference (opt-out keywords or bilingual-output.md reference in body)
  </acceptance_criteria>
  <done>repo-to-paper-skill/SKILL.md exists with valid frontmatter, all 9 required body sections, 4-step workflow with user confirmation checkpoints, scan summary in Step 1, IMRaD/journal H1 in Step 2, content-driven H2 with source annotations in Step 3, H3 with source annotations in Step 4, bilingual eligibility, and line count within ~300-320 budget</done>
</task>

</tasks>

<verification>
After both tasks complete, verify:
1. `test -f references/repo-patterns.md && echo "repo-patterns.md exists"` — PASS
2. `test -f .claude/skills/repo-to-paper-skill/SKILL.md && echo "SKILL.md exists"` — PASS
3. `wc -l references/repo-patterns.md` — should be 80-120 lines
4. `wc -l .claude/skills/repo-to-paper-skill/SKILL.md` — should be 250-330 lines
5. `grep "repo-patterns.md" .claude/skills/repo-to-paper-skill/SKILL.md` — SKILL.md references repo-patterns.md
6. `grep "bilingual-output.md" .claude/skills/repo-to-paper-skill/SKILL.md` — SKILL.md references bilingual spec
7. `grep -c "Step [1-4]" .claude/skills/repo-to-paper-skill/SKILL.md` — should return 4 (four workflow steps)
8. `grep "Category.*Section\|Section.*Category" references/repo-patterns.md` — mapping table exists
</verification>

<success_criteria>
- references/repo-patterns.md exists with file identification patterns and section mapping rules for Python ML repos (~100 lines)
- .claude/skills/repo-to-paper-skill/SKILL.md exists with complete 4-step checkpoint workflow (~300-320 lines)
- SKILL.md follows skill-conventions.md and skill-skeleton.md structure
- SKILL.md references repo-patterns.md and bilingual-output.md in required references
- All 5 requirements addressed: REPO-01 (scan in Step 1), REPO-02 (H1 in Step 2), REPO-03 (H2 in Step 3), REPO-05 (H3 in Step 4), REPO-08 (repo-patterns.md)
</success_criteria>

<output>
After completion, create `.planning/phases/14-repo-to-paper-core-structure/14-01-SUMMARY.md`
</output>
