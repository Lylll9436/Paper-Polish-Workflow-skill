---
phase: 12-askuserquestion-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper-polish-workflow/SKILL.md
autonomous: true
requirements:
  - UXFIX-01
user_setup: []

must_haves:
  truths:
    - "paper-polish-workflow/SKILL.md contains zero occurrences of mcp_ prefix"
    - "paper-polish-workflow/SKILL.md references AskUserQuestion for structured expression selection"
    - "paper-polish-workflow/SKILL.md uses correct tool names Read, Write, Edit"
    - "paper-polish-workflow/SKILL.md follows the skeleton section order: Purpose, Trigger, Modes, References, Ask Strategy, Workflow, Output Contract, Edge Cases, Fallbacks"
    - "Workflow has exactly 4 steps: Collect Context, Structure & Logic Confirmation, Expression Polish & Consistency, Output"
    - "All current sub-operations are preserved in the new 4-step structure (journal style check, highlights, cross-section consistency, reference consultation, read-aloud)"
    - "File is 300 lines or fewer"
  artifacts:
    - path: "paper-polish-workflow/SKILL.md"
      provides: "Restructured Skill file with correct tool names and skeleton-compliant structure"
      min_lines: 150
      contains: "AskUserQuestion"
  key_links:
    - from: "paper-polish-workflow/SKILL.md"
      to: "references/expression-patterns.md"
      via: "references.required frontmatter field"
      pattern: "references/expression-patterns.md"
    - from: "paper-polish-workflow/SKILL.md"
      to: "references/journals/ceus.md"
      via: "references.leaf_hints frontmatter field"
      pattern: "references/journals/ceus.md"
    - from: "paper-polish-workflow/SKILL.md"
      to: "references/anti-ai-patterns.md"
      via: "references.leaf_hints frontmatter field"
      pattern: "references/anti-ai-patterns.md"
---

<objective>
Rewrite `paper-polish-workflow/SKILL.md` to fix all incorrect tool names (`mcp_*` -> correct Claude Code names) and restructure the file to match `skill-skeleton.md` section order, merging the current 6-step workflow into 4 steps, adding all missing convention sections, and compressing from 348 lines to within the 300-line budget.

Purpose: This is a P0 bug fix. The current file references non-existent tool names (`mcp_question`, `mcp_read`, `mcp_write`, `mcp_edit`, `mcp_look_at`) that break structured interaction when users invoke the Skill. Restructuring to match the skeleton ensures consistency with all other Skills in the project.

Output: A fully rewritten `paper-polish-workflow/SKILL.md` that passes all structural and tool-name validation checks.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-askuserquestion-fix/12-CONTEXT.md
@.planning/phases/12-askuserquestion-fix/12-RESEARCH.md
@.planning/phases/12-askuserquestion-fix/12-UI-SPEC.md

@references/skill-skeleton.md
@references/skill-conventions.md
@paper-polish-workflow/SKILL.md

<interfaces>
<!-- Key structural contracts the executor must follow. Extracted from project files. -->

From references/skill-skeleton.md — Required section order:
```
---
[YAML frontmatter]
---
## Purpose
## Trigger
## Modes
## References
  ### Required (always loaded)
  ### Leaf Hints (loaded when needed)
  ### Loading Rules
## Ask Strategy
## Workflow
  ### Step 1: ...
  ### Step 2: ...
  ### Step 3: ...
  ### Step 4: ...
## Output Contract
## Edge Cases
## Fallbacks
```

From references/skill-conventions.md — Frontmatter required fields:
```yaml
name: string          # Must match parent directory: paper-polish-workflow
description: string   # Max 1024 chars, include trigger keywords
triggers: object      # primary_intent + examples (English AND Chinese)
tools: list           # Capability categories: Read, Edit, Structured Interaction
references: object    # required + leaf_hints
input_modes: list     # [file, pasted_text]
output_contract: list # [polished_text, change_summary]
```

From .claude/skills/polish-skill/SKILL.md — Sibling Skill structural reference (197 lines):
- Correct frontmatter format with bilingual triggers
- Correct tool names: Read, Edit, Structured Interaction
- Modes table with default marked
- References split into Required/Leaf Hints/Loading Rules
- Ask Strategy with mode-conditional rules
- Workflow with named steps
- Output Contract table
- Edge Cases table
- Fallbacks table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite paper-polish-workflow/SKILL.md</name>
  <files>paper-polish-workflow/SKILL.md</files>

  <read_first>
    paper-polish-workflow/SKILL.md
    references/skill-skeleton.md
    references/skill-conventions.md
    .claude/skills/polish-skill/SKILL.md
    .planning/phases/12-askuserquestion-fix/12-CONTEXT.md
    .planning/phases/12-askuserquestion-fix/12-RESEARCH.md
    .planning/phases/12-askuserquestion-fix/12-UI-SPEC.md
  </read_first>

  <action>
Perform a COMPLETE REWRITE (not incremental edit) of `paper-polish-workflow/SKILL.md`. Use the Write tool to create the entire file from scratch. The current file is too structurally different from the target skeleton for incremental edits to be safe.

**FRONTMATTER** — Use this exact YAML frontmatter block:

```yaml
---
name: paper-polish-workflow
description: >-
  Systematic top-down workflow for polishing academic papers.
  Structure to logic to expression with user confirmation at each step.
  润色论文、精修论文、学术写作改进。
triggers:
  primary_intent: polish academic paper through structured top-down workflow
  examples:
    - "Polish my paper section by section"
    - "润色论文"
    - "Help me revise my introduction step by step"
    - "精修论文"
    - "Guide me through polishing this draft"
    - "帮我逐步润色这篇论文"
tools:
  - Read
  - Edit
  - Structured Interaction
references:
  required:
    - references/expression-patterns.md
  leaf_hints:
    - references/expression-patterns/introduction-and-gap.md
    - references/expression-patterns/methods-and-data.md
    - references/expression-patterns/results-and-discussion.md
    - references/expression-patterns/conclusions-and-claims.md
    - references/expression-patterns/geography-domain.md
    - references/anti-ai-patterns.md
    - references/journals/ceus.md
input_modes:
  - file
  - pasted_text
output_contract:
  - polished_text
  - change_summary
---
```

**BODY SECTIONS** — Write each section following this exact order. Use `polish-skill/SKILL.md` as the structural exemplar. Below are content requirements for each section:

**## Purpose** (2-3 sentences):
- Top-down systematic workflow (structure before expression)
- User confirmation at each decision point
- Reference-driven expression selection from academic patterns
- Absorbs content from the current "Key Features" section

**## Trigger**:
- "Activates when the user asks to:" with English and Chinese bullet points
- Example invocations matching the frontmatter trigger examples
- Absorbs content from the current "Trigger Conditions" section

**## Modes** — Table with these 4 rows:

| Mode | Default | Behavior |
|------|---------|----------|
| `interactive` | Yes | Full 4-step flow with user confirmation at each decision point |
| `guided` | | Multi-pass with confirmation at key checkpoints only |
| `direct` | | Single-pass polish using defaults; skip AskUserQuestion |
| `batch` | | Same operation applied across multiple sections sequentially |

Default mode: `interactive`. Mode inference: "quickly" or "just fix" -> direct; "step by step" or "逐步" -> interactive (already default).

**## References** — Three subsections:

### Required (always loaded)
| File | Purpose |
|------|---------|
| `references/expression-patterns.md` | Academic expression patterns overview and module index |

### Leaf Hints (loaded when needed)
Table with rows for each expression-patterns leaf, anti-ai-patterns.md, and journals/ceus.md with "When to Load" conditions.

### Loading Rules
- Load expression patterns overview at start; select leaf by section type
- Load anti-AI patterns when polishing expression (Step 3)
- Load journal template when target journal is specified
- Load geography-domain.md when spatial/urban/planning content detected
- If a reference file is missing, warn user and proceed with reduced capability

**## Ask Strategy**:
- Before starting, ask about: (1) target journal, (2) which section to work on, (3) preferred mode if ambiguous
- Rules: never ask more than 3 questions before initial output; in direct mode skip pre-questions; in batch mode skip per-item questions; use Structured Interaction when available, fall back to plain text otherwise
- Reference `skill-conventions.md > AskUserQuestion Enforcement` for full rules

**## Workflow** — Exactly 4 steps with these exact headings:

### Step 1: Collect Context
Sub-operations to include (from current Phase 0 + reference loading):
- Determine input type (file path or pasted text)
- Load required references (expression-patterns overview)
- Identify target journal; load journal template if specified
- Read input content; extract key numbers, claims, data points
- Locate example/reference papers if user provides them (use `Read` tool for PDFs)
- In interactive/guided mode: confirm scope before proceeding

### Step 2: Structure & Logic Confirmation
Sub-operations (from current Steps 1 + 2):
- Analyze section macro structure (e.g., Abstract = Background + Gap + Method + Results + Contribution)
- Present structure table for user confirmation
- Break content into sentences; assign logic function to each
- Present logic chain for user confirmation
- Checkpoint: user confirms structure and logic before expression work begins
- In direct mode: run structure/logic analysis internally, proceed to Step 3 automatically

### Step 3: Expression Polish & Consistency
Sub-operations (from current Steps 3 + 4 + 4.5 + 5 + 5.5 + 5.7 merged):
- Load section-appropriate expression pattern leaf and anti-AI patterns
- For each sentence with expression issues, present 2-3 options via AskUserQuestion (interactive mode) or apply best option automatically (direct mode)
- Include ONE AskUserQuestion pseudocode example using generic placeholders:

```
AskUserQuestion({
  question: "Which expression do you prefer for [sentence function]?",
  options: [
    { label: "[Expression A]", description: "[full sentence with expression A]" },
    { label: "[Expression B]", description: "[full sentence with expression B]" },
    { label: "[Expression C]", description: "[full sentence with expression C]" }
  ]
})
```

- Reference paper consultation: when user questions professionalism, use Read tool to load example papers and extract expression patterns (absorbs current Step 4)
- Journal style check: apply journal-specific requirements from loaded template (absorbs current Step 4.5)
- Repetition and coherence pass: check for repeated expressions, missing transitions; suggest fixes (absorbs current Step 5)
- Cross-section consistency: verify numbers, terminology, claims across sections (absorbs current Step 5.5)

### Step 4: Output
Sub-operations (from current Steps 5.7 + 6 + Write to File merged):
- Generate highlights if journal requires them (absorbs current Step 5.7; for CEUS, see references/journals/ceus.md)
- Suggest read-aloud final check to catch awkward phrasing (absorbs current Step 6)
- Compile all confirmed content into final version
- Present final version with word count for user confirmation
- Write to `*_polished.md` after confirmation (or automatically in batch mode)
- Report word count and any journal constraint notes

**## Output Contract** — Table matching polish-skill pattern:

| Output | Format | Condition |
|--------|--------|-----------|
| `polished_text` | Markdown file (`*_polished.md`) or conversation output | Always produced |
| `change_summary` | Markdown in session | Always produced |
| Word count | Integer | Always reported |
| Journal compliance notes | Bullet list | When a target journal is specified |

**## Edge Cases** — Table with these rows (absorbs current "Common Issue Handling"):

| Situation | Handling |
|-----------|----------|
| Unprofessional word flagged | Present 2-3 alternatives via AskUserQuestion; accept if user insists |
| Section too long for single pass | Split into paragraph-level sub-passes; maintain cross-paragraph coherence |
| No journal specified | Default to general academic style; note in output |
| Mixed language input | Detect dominant language; ask user to confirm target language |
| Reference paper provided as PDF | Use Read tool to load PDF; extract style patterns for expression matching |
| Abrupt sentence transition | Provide transition options via AskUserQuestion |
| Repetition detected between sections | Identify repeated content; suggest which occurrence to rephrase |
| Logic structure needs modification | Return to Step 2 to re-confirm structure before continuing |

**## Fallbacks** — Exact table from 12-UI-SPEC.md:

| Scenario | Fallback |
|----------|----------|
| Structured Interaction unavailable | Ask 1-3 plain-text questions covering highest-impact gaps; do not block workflow |
| Reference file missing | Log the missing file, proceed with reduced capability, warn the user |
| Target journal not specified | Ask once; if declined, use general academic style |
| PDF reference paper unreadable | Ask user to paste relevant excerpts instead |

**SECTIONS TO REMOVE (do NOT include these in the rewrite):**
- "Key Features" (absorbed into Purpose)
- "Core Principles" (distributed into workflow step descriptions)
- "Tools Used" table (replaced by frontmatter `tools` field)
- "Journal-Specific Requirements" (absorbed into References loading rules)
- "Common Issue Handling" (absorbed into Edge Cases)

**LINE BUDGET:**
- Target: 250-290 lines (under 300 hard budget)
- Use concise prose; no verbose markdown formatting
- One AskUserQuestion example only (in Step 3)
- No hardcoded expression content in examples
- End file with standard footer: `*Skill: paper-polish-workflow*` and `*Conventions: references/skill-conventions.md*`

**CRITICAL CHECKS during writing:**
- Zero occurrences of `mcp_` anywhere in the file
- `name` field is exactly `paper-polish-workflow` (not `paper-polish` or `paper-polish-skill`)
- Triggers include both English AND Chinese phrases
- Tools use capability categories: `Read`, `Edit`, `Structured Interaction` (not `AskUserQuestion` in frontmatter -- `AskUserQuestion` goes in the workflow body)
- No `questions` array wrapping in the AskUserQuestion example
- No `multiple: false` field in the AskUserQuestion example
  </action>

  <verify>
    <automated>cd C:/Users/Lylll/Desktop/paper-polish-workflow && grep -c 'mcp_' paper-polish-workflow/SKILL.md; echo "---"; grep -c 'AskUserQuestion' paper-polish-workflow/SKILL.md; echo "---"; grep -cE '\bRead\b|\bWrite\b|\bEdit\b' paper-polish-workflow/SKILL.md; echo "---"; wc -l paper-polish-workflow/SKILL.md; echo "---"; grep -c '### Step' paper-polish-workflow/SKILL.md; echo "---"; grep -c '润色' paper-polish-workflow/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'mcp_' paper-polish-workflow/SKILL.md` returns 0
    - `grep -c 'AskUserQuestion' paper-polish-workflow/SKILL.md` returns >= 2 (at least the example + Ask Strategy reference)
    - `grep -cE '\bRead\b|\bWrite\b|\bEdit\b' paper-polish-workflow/SKILL.md` returns >= 3
    - `wc -l paper-polish-workflow/SKILL.md` returns <= 300
    - `grep -c '### Step' paper-polish-workflow/SKILL.md` returns exactly 4
    - `grep -c '润色' paper-polish-workflow/SKILL.md` returns >= 1 (bilingual triggers present)
    - `head -3 paper-polish-workflow/SKILL.md` shows `---` (frontmatter delimiter)
    - File contains `name: paper-polish-workflow` in frontmatter
    - File contains sections in order: `## Purpose`, `## Trigger`, `## Modes`, `## References`, `## Ask Strategy`, `## Workflow`, `## Output Contract`, `## Edge Cases`, `## Fallbacks`
    - File contains all sub-operations: journal style check, highlights, cross-section consistency, reference consultation, read-aloud
    - File does NOT contain sections: "Key Features", "Core Principles", "Tools Used", "Journal-Specific Requirements", "Common Issue Handling"
    - File contains the Fallbacks table with exactly 4 rows matching UI-SPEC
  </acceptance_criteria>

  <done>
    paper-polish-workflow/SKILL.md is fully rewritten with correct tool names (zero mcp_ occurrences), skeleton-compliant section order, 4-step workflow preserving all sub-operations, valid YAML frontmatter with bilingual triggers, and total line count under 300.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive validation of rewritten SKILL.md</name>
  <files>paper-polish-workflow/SKILL.md</files>

  <read_first>
    paper-polish-workflow/SKILL.md
    references/skill-skeleton.md
    .planning/phases/12-askuserquestion-fix/12-UI-SPEC.md
    .planning/phases/12-askuserquestion-fix/12-VALIDATION.md
  </read_first>

  <action>
Run all validation commands from the 12-VALIDATION.md verification map and perform manual content checks. This task does NOT modify the file -- it only validates and reports.

**Automated checks** (run each command and verify output):

1. `grep -c 'mcp_' paper-polish-workflow/SKILL.md` -- must return exactly `0`
2. `grep -c 'AskUserQuestion' paper-polish-workflow/SKILL.md` -- must return >= 2
3. `grep -cE '\bRead\b|\bWrite\b|\bEdit\b' paper-polish-workflow/SKILL.md` -- must return >= 3
4. `wc -l paper-polish-workflow/SKILL.md` -- must return <= 300
5. `head -50 paper-polish-workflow/SKILL.md` -- must show `---` YAML delimiters and contain fields: `name`, `description`, `triggers`, `tools`, `references`, `input_modes`, `output_contract`
6. `grep -c '### Step' paper-polish-workflow/SKILL.md` -- must return exactly 4
7. `grep -c '润色' paper-polish-workflow/SKILL.md` -- must return >= 1

**Manual content checks** (read the file and verify):

8. Section headers appear in this exact order: `## Purpose`, `## Trigger`, `## Modes`, `## References`, `## Ask Strategy`, `## Workflow`, `## Output Contract`, `## Edge Cases`, `## Fallbacks`
9. The 4 workflow step headings are exactly: `### Step 1: Collect Context`, `### Step 2: Structure & Logic Confirmation`, `### Step 3: Expression Polish & Consistency`, `### Step 4: Output`
10. Sub-operation preservation -- ALL of these concepts appear somewhere in the file:
    - Load references / expression patterns (Step 1)
    - Identify target journal (Step 1)
    - Read input file (Step 1)
    - Extract key numbers/claims (Step 1)
    - Present structure table (Step 2)
    - Confirm logic chain (Step 2)
    - Expression options via AskUserQuestion (Step 3)
    - Reference paper consultation (Step 3)
    - Journal style check (Step 3)
    - Repetition and coherence pass (Step 3)
    - Cross-section consistency (Step 3)
    - Highlights generation (Step 4)
    - Read-aloud suggestion (Step 4)
    - Write to file (Step 4)
    - Report word count (Step 4)
11. The AskUserQuestion example uses generic placeholders (`[Expression A]`, `[Expression B]`, `[Expression C]`), not hardcoded content
12. No `questions` array wrapping in the AskUserQuestion example
13. No `multiple: false` field in the AskUserQuestion example
14. The `name` field in frontmatter is exactly `paper-polish-workflow`
15. The `tools` field lists `Read`, `Edit`, `Structured Interaction` (not `AskUserQuestion` or `Write` or any `mcp_*` name)
16. Edge Cases table has at least 5 rows
17. Fallbacks table has exactly 4 rows matching 12-UI-SPEC.md

If ANY check fails, fix the specific issue in `paper-polish-workflow/SKILL.md` and re-run the failed check until it passes. Do NOT rewrite the entire file for a minor fix -- use Edit tool for targeted corrections.
  </action>

  <verify>
    <automated>cd C:/Users/Lylll/Desktop/paper-polish-workflow && echo "=== mcp check ===" && grep -c 'mcp_' paper-polish-workflow/SKILL.md && echo "=== AskUserQuestion count ===" && grep -c 'AskUserQuestion' paper-polish-workflow/SKILL.md && echo "=== Tool names ===" && grep -cE '\bRead\b|\bWrite\b|\bEdit\b' paper-polish-workflow/SKILL.md && echo "=== Line count ===" && wc -l paper-polish-workflow/SKILL.md && echo "=== Step count ===" && grep -c '### Step' paper-polish-workflow/SKILL.md && echo "=== Bilingual ===" && grep -c '润色' paper-polish-workflow/SKILL.md && echo "=== Frontmatter ===" && head -3 paper-polish-workflow/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - All 7 automated checks pass (mcp=0, AskUserQuestion>=2, tools>=3, lines<=300, steps=4, bilingual>=1, frontmatter has ---)
    - All 10 manual content checks pass (section order, step headings, 15 sub-operations preserved, placeholder format, no wrapping, no multiple field, correct name, correct tools list, edge cases>=5 rows, fallbacks=4 rows)
    - If any fixes were needed, the fix was applied and the check re-run to confirm
  </acceptance_criteria>

  <done>
    All 17 validation checks pass. paper-polish-workflow/SKILL.md is confirmed to meet UXFIX-01 requirements: correct tool names, skeleton-compliant structure, 4-step workflow with all sub-operations preserved, valid frontmatter, and under 300-line budget.
  </done>
</task>

</tasks>

<verification>
All automated verification commands from 12-VALIDATION.md pass:
1. `grep -c 'mcp_' paper-polish-workflow/SKILL.md` returns 0
2. `grep -c 'AskUserQuestion' paper-polish-workflow/SKILL.md` returns >= 2
3. `grep -cE '\bRead\b|\bWrite\b|\bEdit\b' paper-polish-workflow/SKILL.md` returns >= 3
4. `wc -l paper-polish-workflow/SKILL.md` returns <= 300
5. `grep -c '### Step' paper-polish-workflow/SKILL.md` returns exactly 4
6. `grep -c '润色' paper-polish-workflow/SKILL.md` returns >= 1
7. Section order matches skeleton exactly
8. All 15 sub-operations from the current 6-step workflow are present in the new 4-step structure
</verification>

<success_criteria>
- paper-polish-workflow/SKILL.md uses AskUserQuestion (not mcp_question) for all structured questions
- All tool references use correct Claude Code tool names (Read, Write, Edit -- not mcp_read, mcp_write, mcp_edit)
- File structure matches skill-skeleton.md section order
- Workflow has exactly 4 steps (not 6)
- File is within 300-line budget
- YAML frontmatter contains all required fields with bilingual triggers
- No workflow functionality lost from the 6-to-4 step consolidation
</success_criteria>

<output>
After completion, create `.planning/phases/12-askuserquestion-fix/12-01-SUMMARY.md`
</output>
