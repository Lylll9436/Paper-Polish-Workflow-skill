---
phase: 09-literature-support-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/visualization-skill/SKILL.md
autonomous: true
requirements:
  - SUPP-03
must_haves:
  truths:
    - "Visualization Skill produces exactly 2–3 numbered recommendation cards; each card contains three items: chart type name, reason it fits the user's specific data/question, and Python/R tool hints"
    - "When the data description mentions spatial coordinates, regions, administrative boundaries, or equivalent spatial signals, the Skill proactively includes choropleth map, spatial scatter plot, or kernel density map as candidates"
    - "When the data description has no spatial signals, the Skill recommends only non-spatial chart types (bar, line, scatter, heatmap, etc.) — it never forces geography charts onto non-spatial data"
    - "Tool hints favor the geography/urban science stack when spatial data is detected: geopandas, contextily, pysal, QGIS for Python; sf, ggplot2, tmap for R"
    - "No code generation: tool name hints only — the Skill names the library and function, not a code block"
    - "Output is displayed in conversation; no file write"
    - "The Skill stays under 300 lines and follows all skill-conventions.md frontmatter and section requirements"
  artifacts:
    - path: ".claude/skills/visualization-skill/SKILL.md"
      provides: "Chart type recommendation Skill with geography-aware detection"
      exports: ["recommendation cards (2-3)", "reason-fit explanation", "tool hints", "spatial vs. non-spatial detection"]
      max_lines: 300
  key_links:
    - from: ".claude/skills/visualization-skill/SKILL.md"
      to: "user data description (pasted text)"
      via: "Ask Strategy: collect data type + research question before recommending"
      pattern: "data description|research question"
    - from: ".claude/skills/visualization-skill/SKILL.md"
      to: "spatial signal detection"
      via: "keyword scan of data description: 'coordinates|region|district|admin|polygon|latitude|longitude|spatial'"
      pattern: "spatial|choropleth|geopandas"
---

<objective>
Author the Visualization Skill SKILL.md for .claude/skills/visualization-skill/.

Purpose: Deliver SUPP-03 — a Skill that accepts a data description and research question, detects whether spatial data is involved, and returns 2–3 structured chart type recommendations with rationale and tool hints. Geography-specific chart types are included only when the data description contains spatial signals.

Output: .claude/skills/visualization-skill/SKILL.md — a complete, convention-compliant Skill under 300 lines.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/skill-conventions.md
@references/skill-skeleton.md
@.claude/skills/logic-skill/SKILL.md
@.planning/phases/09-literature-support-skills/09-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts and patterns the executor must follow. Extracted from codebase. -->

From references/skill-conventions.md — Required SKILL.md sections (all mandatory):
  ## Purpose, ## Trigger, ## Modes, ## References, ## Ask Strategy,
  ## Workflow, ## Output Contract, ## Edge Cases, ## Fallbacks, ## Examples

From references/skill-conventions.md — Frontmatter required fields:
  name, description, triggers (primary_intent + examples), tools, references
  (required + leaf_hints), input_modes, output_contract

Frontmatter shape (LOCKED from RESEARCH.md):
  name: visualization-skill
  description: >-
    Recommend appropriate chart types for experimental data with rationale and tool hints.
    Geography-aware: choropleth, spatial scatter, kernel density when spatial data detected.
    为实验数据推荐合适的图表类型，支持地理空间数据可视化建议。
  triggers:
    primary_intent: recommend visualization types for experimental data
    examples:
      - "What chart should I use for this data?"
      - "帮我选择合适的可视化方式"
      - "Recommend a visualization for my spatial analysis results"
      - "我应该用什么图展示这个结果"
      - "Which plot type fits my regression results?"
      - "帮我推荐一个展示时空数据的图表"
  tools:
    - Structured Interaction
  references:
    required: []
    leaf_hints: []
  input_modes:
    - pasted_text
  output_contract:
    - recommendation_list

Recommendation card format (LOCKED from RESEARCH.md):
  **Recommendation 1: [Chart Type Name]**
  - Why it fits: [1–2 sentences directly referencing the user's data type and research question]
  - Tool hints: Python: [library.function()]; R: [package + geom/function]

Spatial signal keywords (if ANY of these appear in data description, include spatial chart candidates):
  "coordinates", "latitude", "longitude", "spatial", "region", "district",
  "administrative", "admin", "polygon", "raster", "GIS", "map", "geospatial",
  "urban", "city boundaries", "study area", "choropleth"

Geography/urban science tool stack (use when spatial signals detected):
  Python: geopandas, contextily, pysal, folium, matplotlib, QGIS
  R: sf, ggplot2, tmap, leaflet

General (non-spatial) tool stack:
  Python: matplotlib, seaborn, plotly, scipy
  R: ggplot2, ggpubr, base R

Anti-patterns (NEVER do these):
  - Generate Python/R code blocks — tool name hints only
  - Recommend choropleth/spatial scatter for non-spatial data
  - Output fewer than 2 or more than 3 recommendations
  - Write to a file — output is conversation-only
  - Ask user to provide an actual data file — accept text description only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author visualization-skill SKILL.md</name>
  <files>.claude/skills/visualization-skill/SKILL.md</files>
  <action>
Create the directory .claude/skills/visualization-skill/ and write SKILL.md.

Start from the frontmatter shape documented in the interfaces block above (name: visualization-skill, tools: Structured Interaction only, references.required: [], leaf_hints: [], input_modes: pasted_text, output_contract: recommendation_list).

Write all required sections per skill-conventions.md:

**## Purpose**
One paragraph: Skill accepts a plain-language description of experimental data (data type, variables, sample size) and a research question (what the figure must communicate), then recommends 2–3 chart types ordered by fit. Each recommendation includes the chart type name, a 1–2 sentence rationale referencing the user's specific data and question, and Python/R library hints (names only, no code). The Skill is geography-aware: if the data description contains spatial signals (coordinates, regions, administrative boundaries, GIS data), it proactively includes choropleth maps, spatial scatter plots, or kernel density maps as candidates. The Skill accepts only text descriptions — it does not read or process actual data files.

**## Trigger**
Activates when user asks to choose a chart type, recommend a visualization, or decide how to display experimental results. Include 6 bilingual examples (English + Chinese) covering: ask what chart to use for specific data, recommend a visualization for spatial results, help choose a plot type, 帮我选择可视化方式, 我应该用什么图, 帮我推荐图表类型.

**## Modes**
Table with two rows:
- direct (default): single-pass recommendation after Ask Strategy; 2–3 cards delivered in one response
- batch: NOT supported — each recommendation requires specific data description and research question

**## References**
Required table: none.
Leaf hints: none.
Note: "No reference files loaded. Recommendations derived from data description and research question provided by the user."

**## Ask Strategy**
Two required inputs; both must be present before recommendations:
1. Data description: what is the data? (data type, key variables, spatial or non-spatial, sample size if relevant) — ask if not in trigger
2. Research question for this figure: what should the chart communicate? (e.g., distribution, comparison, trend, spatial pattern, correlation) — ask if not in trigger

If user provides only data description or only research question, ask for the missing input before proceeding. Do not generate recommendations without both inputs.

**## Workflow**
Three steps:

Step 1 — Collect Context:
- Extract data description and research question from trigger if present; ask for any missing inputs (Ask Strategy above)

Step 2 — Detect Spatial Signals and Select Chart Candidates:
- Scan the data description for spatial signal keywords: "coordinates", "latitude", "longitude", "spatial", "region", "district", "administrative", "polygon", "GIS", "map", "geospatial", "study area", "choropleth"
- If spatial signals present: include geography chart types as candidates (choropleth map, spatial scatter plot, kernel density map, hexbin map) alongside general types
- If no spatial signals: use only general chart types (bar chart, grouped bar, line chart, scatter plot, box plot, violin plot, heatmap, regression plot)
- Select 2–3 chart types ordered by fit — the best fit comes first
- Use the research question to break ties: "distribution" favors histogram/box; "comparison" favors bar; "trend" favors line; "correlation" favors scatter; "spatial pattern" favors choropleth/spatial scatter

Step 3 — Generate Recommendation Cards:
- Write 2–3 cards using the locked format from the interfaces block
- Reason must reference the user's specific data and question — not generic descriptions
- Tool hints: when spatial data detected, include geopandas/contextily/tmap; when non-spatial, include matplotlib/seaborn/ggplot2
- No code blocks — library and function names only (e.g., "geopandas.plot()", "seaborn.boxplot()")

**## Output Contract**
Table with output type:
- recommendation_list: 2–3 numbered recommendation cards displayed in conversation (always — no file write)

Include one concrete spatial example showing 3 cards (choropleth, spatial scatter, kernel density) and one non-spatial example showing 2 cards (bar chart, violin plot), demonstrating how tool hints differ between spatial and non-spatial cases.

**## Edge Cases**
Table covering:
- Data description has no spatial signals but user explicitly requests a map → explain why a map may not be appropriate for non-spatial data, offer to recommend spatial chart if user confirms data has a spatial dimension
- Data description mentions "urban" or "city" without explicit spatial coordinates → treat as a boundary case: ask "Does your data include spatial coordinates or administrative unit boundaries?" before including spatial chart types
- Only one data variable mentioned → acknowledge limited dimensionality; recommend univariate charts (histogram, bar) that suit the constraint
- User provides a file path instead of data description → ask user to describe the data instead: "Please describe your data (type, variables, sample size) — I cannot read data files directly"
- User asks for "best" chart without context → run Ask Strategy; do not guess from insufficient information
- Research question is ambiguous ("show the results") → ask for clarification: "What aspect of the results should the figure communicate — distribution, comparison, trend, or spatial pattern?"

**## Fallbacks**
Table covering:
- Structured Interaction unavailable → ask data description and research question as two sequential plain-text questions; proceed with same recommendation logic
- Data description too vague to determine spatial vs. non-spatial → default to non-spatial chart types; note: "If your data has a spatial dimension, include that in the description for geography-aware recommendations"
- Research question not answerable by a single chart type → acknowledge and recommend the chart type that covers the primary question; note secondary question as a separate visualization candidate

**## Examples**
Two examples:

Example 1 (spatial data):
- User: "I have district-level urban green space coverage data for 50 districts in Shenzhen. Research question: how does green space vary spatially across districts?"
- Show: spatial signals detected (district, Shenzhen) → 3 recommendations: choropleth map, spatial scatter, kernel density. Show tool hints using geopandas and tmap.

Example 2 (non-spatial data):
- User: "I have survey responses from 500 residents on satisfaction scores (1–5) for three neighborhoods. Research question: how do satisfaction scores compare across neighborhoods?"
- Show: no spatial signals → 2 recommendations: grouped bar chart, violin plot. Show tool hints using seaborn and ggplot2. No geography charts.

Line budget: aim for 210–250 lines. The two examples are the largest section — keep each example as a compact 6–8 line illustration.
  </action>
  <verify>
    <automated>wc -l .claude/skills/visualization-skill/SKILL.md && grep -n "^## " .claude/skills/visualization-skill/SKILL.md</automated>
  </verify>
  <done>
File exists at .claude/skills/visualization-skill/SKILL.md, line count is under 300, and all required sections (Purpose, Trigger, Modes, References, Ask Strategy, Workflow, Output Contract, Edge Cases, Fallbacks, Examples) are present with the correct ## heading level.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate visualization-skill conventions compliance</name>
  <files>.claude/skills/visualization-skill/SKILL.md</files>
  <action>
Run structural validation on the authored SKILL.md. Fix any failures before marking complete.

1. Line count: `wc -l .claude/skills/visualization-skill/SKILL.md` — must be ≤ 300.
2. Required sections: `grep -n "^## " .claude/skills/visualization-skill/SKILL.md` — verify all required headings: Purpose, Trigger, Modes, References, Ask Strategy, Workflow, Output Contract, Edge Cases, Fallbacks, Examples.
3. Recommendation card format: `grep -c "Recommendation [0-9]\|Why it fits\|Tool hints" .claude/skills/visualization-skill/SKILL.md` — must return ≥ 3.
4. Spatial detection: `grep -c "spatial\|choropleth\|geopandas\|coordinates" .claude/skills/visualization-skill/SKILL.md` — must return ≥ 4 (spatial awareness is a core SUPP-03 requirement).
5. Non-spatial path: `grep -c "non-spatial\|no spatial\|seaborn\|matplotlib" .claude/skills/visualization-skill/SKILL.md` — must return ≥ 2 (non-spatial recommendations must also be covered).
6. No code generation: `grep -c "```python\|```r\b" .claude/skills/visualization-skill/SKILL.md` — must return 0 (tool name hints only, no code blocks for generated recommendations; the interfaces section may contain short illustrative hints but should also use inline format).
7. Output is conversation-only: `grep -c "Write tool\|file write\|_visualization" .claude/skills/visualization-skill/SKILL.md` — must return 0 (no file output).
8. Bilingual triggers: `grep -c "帮我\|可视化\|图表" .claude/skills/visualization-skill/SKILL.md` — must return ≥ 2.
9. 2–3 recommendations constraint: `grep -c "2.3\|2–3\|two.*three\|three.*two" .claude/skills/visualization-skill/SKILL.md` — must return ≥ 1.
10. Frontmatter check: confirm name, description, triggers, tools, references, input_modes, output_contract all appear in YAML frontmatter block.

Document each check result. Fix any failures immediately before completing this task.

Note on check 6: the interfaces section in the PLAN references code-like patterns in an illustrative comment — this is the PLAN, not the SKILL.md. The SKILL.md must not contain ```python or ```r fenced code blocks in the generated recommendations section. Verify specifically the Workflow, Output Contract, and Examples sections contain no fenced code blocks.
  </action>
  <verify>
    <automated>wc -l .claude/skills/visualization-skill/SKILL.md && grep -c "^## " .claude/skills/visualization-skill/SKILL.md && grep -c "choropleth\|geopandas\|spatial" .claude/skills/visualization-skill/SKILL.md && grep -c "帮我\|可视化\|图表" .claude/skills/visualization-skill/SKILL.md</automated>
  </verify>
  <done>
All structural checks pass: line count ≤ 300; all required sections present; recommendation card format elements present ≥ 3 times; spatial detection terminology ≥ 4 times; non-spatial path documented; no fenced code blocks in recommendations; no file write references; at least 2 Chinese trigger phrases; 2–3 recommendations constraint noted; all frontmatter required fields confirmed in YAML block.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `wc -l .claude/skills/visualization-skill/SKILL.md` returns ≤ 300
- `grep -n "^## " .claude/skills/visualization-skill/SKILL.md` shows: Purpose, Trigger, Modes, References, Ask Strategy, Workflow, Output Contract, Edge Cases, Fallbacks, Examples
- The Workflow Step 2 shows explicit spatial signal detection logic
- The Examples section shows both a spatial data example (choropleth in recommendations) and a non-spatial example (bar chart in recommendations)
- The Output Contract specifies conversation-only output with no file write
- The Ask Strategy requires both data description AND research question before proceeding
</verification>

<success_criteria>
.claude/skills/visualization-skill/SKILL.md exists, passes all structural checks, and fully implements SUPP-03: users receive 2–3 chart type recommendation cards with rationale and tool hints, spatial data signals automatically trigger geography-specific chart candidates (choropleth, spatial scatter, kernel density), non-spatial data receives only appropriate general chart types, and output is delivered in conversation with no file write and no code generation.
</success_criteria>

<output>
After completion, create .planning/phases/09-literature-support-skills/09-03-SUMMARY.md
</output>
